<%# [turuta] Textos en config/locales/turuta.*.yml, no escritos aqui en ingles. %>
<p><%= t('turuta.mail.password_change.greeting', email: @resource.email) %></p>

<p><%= t('turuta.mail.password_change.intro') %></p>
