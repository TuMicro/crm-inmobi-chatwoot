<%# [turuta] Textos en config/locales/turuta.*.yml, no escritos aqui en ingles. %>
<p><%= t('turuta.mail.reset_password.greeting', email: @resource.email) %></p>

<p><%= t('turuta.mail.reset_password.intro') %></p>

<p><%= link_to t('turuta.mail.reset_password.action'), frontend_url('auth/password/edit', reset_password_token: @token) %></p>

<p><%= t('turuta.mail.reset_password.ignore') %></p>
<p><%= t('turuta.mail.reset_password.unchanged') %></p>
