<%# [turuta] Textos en config/locales/turuta.*.yml, no escritos aqui en ingles. %>
<p><%= t('turuta.mail.unlock.greeting', email: @resource.email) %></p>

<p><%= t('turuta.mail.unlock.intro') %></p>

<p><%= t('turuta.mail.unlock.instruction') %></p>

<p><%= link_to t('turuta.mail.unlock.action'), unlock_url(@resource, unlock_token: @token) %></p>
